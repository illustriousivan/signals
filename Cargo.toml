[package]
name = "signals"
version = "0.1.0"
edition = "2024"

[dependencies]
indexmap = "2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "signals_bench"
harness = false
